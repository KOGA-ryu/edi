# Review Gate Feature

Reserved generic route-attached review feature. The current working implementation lives in `src/features/ui_taxonomy/` because the first subject is Draftsman reviewing its own UI.

